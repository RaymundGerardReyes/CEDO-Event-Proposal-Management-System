// Simple test to verify Jest is working
test('simple test', () => {
    expect(1 + 1).toBe(2);
});

describe('Simple test suite', () => {
    it('should pass', () => {
        expect(true).toBe(true);
    });
}); 